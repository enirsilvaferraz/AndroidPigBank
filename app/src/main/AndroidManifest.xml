<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.system.androidpigbank">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".controllers.activities.CategoryManagerActivity"
            android:label="@string/title_activity_category_manager"
            android:theme="@style/AppTheme.Green" />

        <activity
            android:name=".controllers.activities.CategoryListActivity"
            android:label="@string/title_activity_category_manager"
            android:theme="@style/AppTheme.Green" />

        <activity
            android:name=".controllers.activities.HomeActivity"
            android:label="@string/title_app"
            android:theme="@style/AppTheme.Blue">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".models.business.BackupService"
            android:exported="false" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="8770749f6521240daa266f6d44eba27eb8f2e4ee" />
    </application>

</manifest>